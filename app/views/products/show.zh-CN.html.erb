<div class="container product_showpage">

  <div class="text-center" >
    <h2 id="title"> <%= @product.title %> </h2>
  </div>
  <div class="pull-right" style="margin-top:20px;" id="product-collect-<%= @product.id %>">
    <%= render "collect" %>
  </div>
  <br>
  <div class="text-center" >
    <div class="short-line"></div>
  </div>

  <div>
    <p class="text-left">
      简述：<%= sanitize @product.description %> <br><br>

      <hr>

      <%= sanitize @product.body %> <br><br>

      <div class="text-left">
        <% if @photos.present? %>
          <% @photos.each_with_index do |p, index| %>
            <% if index == 1 %>
              <%= image_tag p.avatar.thumb.url %>
            <% end %>
          <% end %>
        <% else %>
            <%= image_tag("http://placehold.it/200x200&text=No Pic", class: "thumbnail") %>
        <% end %>
      </div>
    </p>


  </div>
  <br>


  <hr>

  <div class="product-price text-left"> 价格: ￥ <%= @product.price %> </div>
  <div class="pull-right">
      <%= link_to(t("add_to_cart"), add_to_cart_product_path(@product), :remote => true, method: :post, :class => "btn btn-danger") %>
  </div>
  <br>
  <hr>
  <div class="pull-left ">
      <p><i class="fa fa-share-alt" aria-hidden="true"></i> 分享 </p>
  </div>
  <div class="col-md-4" style="margin-left: 50px;">
    <%= social_share_button_tag(@product.title) %>
  </div>

  <br>
  <br>
  <br>
  <br>
  <br>
  <br>
  <hr>

  <div class="text-center">
    <% if @product.comments.blank? %>
      <h3>这个经验还没有人评论，快来为它添加一个吧！</h3>
    <% else %>
      <h3>这个经验已经有<%= @product.comments_count %> 个评论了，快来添加你的评论吧！</h3>
    <% end %>
  </div>
  <div class="short-line"></div>
  <div id="comments">
    <%= render @product.comments%>

    <%= render "comments/form"%>
  </div>
</div>

<% console%>
